import Vue from 'vue'

import { NavBar, Field, Button, Row, Icon, Cell, CellGroup, Toast } from 'vant'

Vue.use(NavBar)// 导航栏
  .use(Field)// 输入框
  .use(Button)// 按钮
  .use(Row)// 行
  .use(Icon)// 图标
  .use(Cell)// 单元格
  .use(CellGroup)// 单元组
  .use(Toast)// 轻提示
